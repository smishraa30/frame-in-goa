# Both optional — the app runs without either.

# Attach a Vercel Blob store to persist shared cards so /s/<id> link previews
# survive. Without it, publishing falls back to the serverless tmpdir.
BLOB_READ_WRITE_TOKEN=

# Canonical origin used in OG tags. Auto-derived on Vercel.
NEXT_PUBLIC_SITE_URL=
